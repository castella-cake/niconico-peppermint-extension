.playersettings-container {
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    width: 30%;
    height: 100%;
    background: rgba(0,0,0,0.867);
    backdrop-filter: blur(8px);
    padding: 16px;
    z-index: 2;
    
}
.playersettings-title {
    font-size: 1.2em;
    font-weight: bold;
    margin-bottom: 8px;
}